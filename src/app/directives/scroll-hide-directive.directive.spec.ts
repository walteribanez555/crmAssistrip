import { ScrollHideDirectiveDirective } from './scroll-hide-directive.directive';

describe('ScrollHideDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollHideDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
